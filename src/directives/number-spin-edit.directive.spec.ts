import { NumberSpinEditDirective } from './number-spin-edit.directive';

describe('NumberSpinEditDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberSpinEditDirective();
    expect(directive).toBeTruthy();
  });
});
